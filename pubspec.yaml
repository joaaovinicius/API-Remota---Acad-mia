name: myapp
description: "A new Flutter project."
publish_to: 'none' # Remove esta linha se você desejar publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  cupertino_icons: ^1.0.8
  logging: ^1.0.2 # Adicionado para suporte a logging

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/

